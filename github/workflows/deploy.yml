name: MOG_TOTALITY_DEPLOY

on:
  push:
    branches: ["main"] # Triggers on every push to the main branch

# STRATEGIC FIX: Prevents "Cancelled" status by queuing deployments
concurrency:
  group: "pages"
  cancel-in-progress: false 

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Points to the root where index.html and logo.png reside
          path: '.' 

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        name: MOG_TOTALITY_DEPLOY

on:
  push:
    branches: ["main"] # Triggers on every push to the main branch

# STRATEGIC FIX: Prevents "Cancelled" status by queuing deployments
concurrency:
  group: "pages"
  cancel-in-progress: false 

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Points to the root where index.html and logo.png reside
          path: '.' 

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
